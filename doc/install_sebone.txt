
sudo apt-get update
sudo apt-get upgrade
sudo apt-get install git
curl -sL https://deb.nodesource.com/setup_8.x | sudo -E bash -
sudo apt-get install -y nodejs
mkdir tcvb; cd tcvb
git init
git config --global user.name "spardo"
git config --global user.email "sebastien.pardo@cegetel.net"
git remote add origin https://github.com/spardo776/tcvb
git pull origin master
npm install express
sudo npm install -g nodemon
mkdir data
cp -r data_sample/* data/
# copier clé publique dans .ssh/authorized_keys
chmod 600 .ssh/*
chmod 700 .ssh
...
...
git add <file_or_dir> ...
git commit -a -m <comment>
git push origin master

